# Flowree Solver

Simple flowfree solver wirten in Rust.

## Usage

Clone the repository onto your computer. A recent rust toolchain needs to be installed on your computer in order to compile the project.

To use, run:

```sh
flowfree [Option] [Path to Puzzle File]
```

Options for this program are:

```
-h: Prints a help message and exits.
```
